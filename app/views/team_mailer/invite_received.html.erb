<%= transactional_layout_v2 do %>
  <div class='transactional-v2-layout-main'>
    <%= render partial: 'header', locals: { text: "You have been invited to a hub" } %>
    <%= render partial: 'product_card' %>

    <%= render layout: 'content' do %>
      <%= user_link(@referrer) %> has invited you to join <%= product_link(@product) %>.
      Click the link below to reply to this invite.
    <% end %>

    <%= render partial: 'cta', locals: { text: "Open Invite", url: @invite_url, show_link_text: true } %>

    <%= render layout: 'content', locals: { class_name: 'sm-text' } do %>
      If you don’t know this user or this looks like spam, please flag this request to our team at <%= support_mail_link %>.
    <% end %>
  </div>

  <%= render partial: 'footer', locals: { text: "you have been invited to a hub" } %>
<% end %>
