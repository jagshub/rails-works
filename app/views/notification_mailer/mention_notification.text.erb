Hey there, <%= @user.first_name  %>!

<%= @from_user.name %> just mentioned you in  <%= @comment.subject_name %>:

<%= raw BetterFormatter.call(@comment.body, mode: :simple) %>

Replay on Product Hunt -  <%= comment_url(@comment, @tracking_params.merge(comment: @comment.id))  %>

------------------------------------

You can [opt out](<%= @unsubscribe_url %>) of mention notifications or manage all of your email notifications from [your profile](<%= Routes.my_settings_url %>).